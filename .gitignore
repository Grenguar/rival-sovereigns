node_modules/
dist/
coverage/
.DS_Store
*.log
.vite/
test-results/
playwright-report/
.pnpm-store/

# Fetched third-party archives — tools/fetch-assets.ts re-downloads and
# SHA-256 verifies these; the manifest is the checked-in source of truth.
assets/vendor/

# Playwright
test-results/
playwright-report/
blob-report/
.playwright/

# Local run artefacts
game-screenshot*.png

# Research inputs are local-only. Record URLs, authors and licenses in tracked files
# under art/reference/, but never commit raw captures by accident.
art/reference/raw/

# tsc -b incremental state
tsconfig.tsbuildinfo
